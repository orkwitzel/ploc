# ploc Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a small optimized Rust CLI named `ploc` that reports LOC and language usage for exactly the current working directory.

**Architecture:** The binary parses a few flags, asks a scanner module to collect language statistics from `.`, then passes a summary to a renderer module for onefetch-inspired terminal output. The scanner owns noise-directory exclusion and uses `tokei` for language-aware counting.

**Tech Stack:** Rust 2021, `clap` for CLI parsing, `tokei` for LOC counting, `owo-colors` for ANSI styling, `assert_cmd`/`assert_fs`/`predicates` for integration tests.

---

## File Structure

- `Cargo.toml`: package metadata, dependencies, optimized release profile.
- `src/main.rs`: CLI entrypoint, argument parsing, error-to-exit handling.
- `src/lib.rs`: shared module exports for tests and binary.
- `src/scan.rs`: current-directory scan configuration, noise exclusions, summary model.
- `src/render.rs`: compact terminal output and color/no-color formatting.
- `tests/cli.rs`: end-to-end CLI behavior using temporary fixture directories.
- `README.md`: install and usage notes.

### Task 1: Scaffold Rust Crate

**Files:**
- Create: `Cargo.toml`
- Create: `src/main.rs`
- Create: `src/lib.rs`
- Create: `.gitignore`

- [ ] **Step 1: Create minimal package files**

```toml
# Cargo.toml
[package]
name = "ploc"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]

[dev-dependencies]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
```

```rust
// src/main.rs
fn main() {
    println!("ploc");
}
```

```rust
// src/lib.rs
```

```gitignore
/target
```

- [ ] **Step 2: Verify scaffold builds**

Run: `cargo test`

Expected: build succeeds with zero tests.

- [ ] **Step 3: Commit**

```bash
git add Cargo.toml src/main.rs src/lib.rs .gitignore
git commit -m "chore: scaffold rust cli"
```

### Task 2: Add CLI Contract

**Files:**
- Modify: `Cargo.toml`
- Modify: `src/main.rs`
- Test: `tests/cli.rs`

- [ ] **Step 1: Write failing CLI tests**

```rust
// tests/cli.rs
use assert_cmd::Command;
use predicates::prelude::*;

#[test]
fn help_lists_supported_flags() {
    let mut cmd = Command::cargo_bin("ploc").unwrap();

    cmd.arg("--help")
        .assert()
        .success()
        .stdout(predicate::str::contains("--include-noise"))
        .stdout(predicate::str::contains("--no-color"));
}

#[test]
fn version_prints_package_version() {
    let mut cmd = Command::cargo_bin("ploc").unwrap();

    cmd.arg("--version")
        .assert()
        .success()
        .stdout(predicate::str::contains(env!("CARGO_PKG_VERSION")));
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cargo test --test cli`

Expected: FAIL because `assert_cmd`, `predicates`, and the CLI flags are not implemented.

- [ ] **Step 3: Add dependencies and minimal parser**

```toml
# Cargo.toml additions
[dependencies]
clap = { version = "4.5", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.1"
assert_fs = "1.1"
```

```rust
// src/main.rs
use clap::Parser;

#[derive(Debug, Parser)]
#[command(version, about = "Count LOC and languages in the current directory")]
struct Cli {
    /// Include dependency, build, VCS, cache, and generated-output directories.
    #[arg(long)]
    include_noise: bool,

    /// Disable ANSI color.
    #[arg(long)]
    no_color: bool,
}

fn main() {
    let _cli = Cli::parse();
    println!("ploc");
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cargo test --test cli`

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add Cargo.toml Cargo.lock src/main.rs tests/cli.rs
git commit -m "feat: add cli flags"
```

### Task 3: Implement Scanning Summary

**Files:**
- Modify: `Cargo.toml`
- Modify: `src/lib.rs`
- Create: `src/scan.rs`
- Test: `tests/cli.rs`

- [ ] **Step 1: Write failing scan behavior tests**

```rust
// append to tests/cli.rs
use assert_fs::prelude::*;

#[test]
fn scans_only_current_directory() {
    let temp = assert_fs::TempDir::new().unwrap();
    temp.child("parent.rs").write_str("fn outside() {}\n").unwrap();
    let child = temp.child("child");
    child.create_dir_all().unwrap();
    child.child("inside.rs").write_str("fn inside() {}\n").unwrap();

    let mut cmd = Command::cargo_bin("ploc").unwrap();
    cmd.current_dir(child.path())
        .arg("--no-color")
        .assert()
        .success()
        .stdout(predicate::str::contains("LOC       1"))
        .stdout(predicate::str::contains("Files     1"));
}

#[test]
fn excludes_noise_directories_by_default() {
    let temp = assert_fs::TempDir::new().unwrap();
    temp.child("src.rs").write_str("fn main() {}\n").unwrap();
    temp.child("target/generated.rs")
        .write_str("fn generated() {}\n")
        .unwrap();

    let mut cmd = Command::cargo_bin("ploc").unwrap();
    cmd.current_dir(temp.path())
        .arg("--no-color")
        .assert()
        .success()
        .stdout(predicate::str::contains("LOC       1"))
        .stdout(predicate::str::contains("Files     1"));
}

#[test]
fn include_noise_counts_noise_directories() {
    let temp = assert_fs::TempDir::new().unwrap();
    temp.child("src.rs").write_str("fn main() {}\n").unwrap();
    temp.child("target/generated.rs")
        .write_str("fn generated() {}\n")
        .unwrap();

    let mut cmd = Command::cargo_bin("ploc").unwrap();
    cmd.current_dir(temp.path())
        .args(["--no-color", "--include-noise"])
        .assert()
        .success()
        .stdout(predicate::str::contains("LOC       2"))
        .stdout(predicate::str::contains("Files     2"));
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cargo test --test cli`

Expected: FAIL because scanning is not implemented.

- [ ] **Step 3: Add `tokei` and scanner module**

```toml
# Cargo.toml addition
tokei = "12.1"
```

```rust
// src/lib.rs
pub mod scan;
```

```rust
// src/scan.rs
use std::path::Path;
use tokei::{Config, Languages};

const NOISE_DIRS: &[&str] = &[
    ".git",
    ".hg",
    ".svn",
    "node_modules",
    "target",
    "dist",
    "build",
    ".next",
    ".nuxt",
    "vendor",
    ".cache",
];

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct LanguageStat {
    pub name: String,
    pub code: usize,
    pub files: usize,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Summary {
    pub root_name: String,
    pub total_code: usize,
    pub total_files: usize,
    pub languages: Vec<LanguageStat>,
}

pub fn scan_current_dir(include_noise: bool) -> Result<Summary, String> {
    let root = std::env::current_dir().map_err(|err| format!("cannot read current directory: {err}"))?;
    scan_path(&root, include_noise)
}

pub fn scan_path(root: &Path, include_noise: bool) -> Result<Summary, String> {
    if !root.is_dir() {
        return Err(format!("{} is not a directory", root.display()));
    }

    let mut config = Config::default();
    let mut ignored_directories = Vec::new();
    if !include_noise {
        ignored_directories.extend(NOISE_DIRS.iter().map(|dir| (*dir).to_string()));
    }
    config.for_each_fn = None;
    config.ignored_directories = Some(ignored_directories);

    let mut languages = Languages::new();
    languages.get_statistics(&[root], &[], &config);

    let mut stats = languages
        .into_iter()
        .filter_map(|(language, reports)| {
            let code = reports.code;
            let files = reports.reports.len();
            (code > 0 || files > 0).then(|| LanguageStat {
                name: language.name().to_string(),
                code,
                files,
            })
        })
        .collect::<Vec<_>>();

    stats.sort_by(|left, right| {
        right
            .code
            .cmp(&left.code)
            .then_with(|| left.name.cmp(&right.name))
    });

    let total_code = stats.iter().map(|stat| stat.code).sum();
    let total_files = stats.iter().map(|stat| stat.files).sum();
    let root_name = root
        .file_name()
        .and_then(|name| name.to_str())
        .unwrap_or(".")
        .to_string();

    Ok(Summary {
        root_name,
        total_code,
        total_files,
        languages: stats,
    })
}
```

- [ ] **Step 4: Wire scanner into `main` with temporary output**

```rust
// src/main.rs
use clap::Parser;
use ploc::scan::scan_current_dir;

#[derive(Debug, Parser)]
#[command(version, about = "Count LOC and languages in the current directory")]
struct Cli {
    /// Include dependency, build, VCS, cache, and generated-output directories.
    #[arg(long)]
    include_noise: bool,

    /// Disable ANSI color.
    #[arg(long)]
    no_color: bool,
}

fn main() {
    let cli = Cli::parse();

    match scan_current_dir(cli.include_noise) {
        Ok(summary) => {
            println!("Project   {}", summary.root_name);
            println!("LOC       {}", summary.total_code);
            println!("Files     {}", summary.total_files);
        }
        Err(message) => {
            eprintln!("ploc: {message}");
            std::process::exit(1);
        }
    }
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `cargo test --test cli`

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add Cargo.toml Cargo.lock src/lib.rs src/main.rs src/scan.rs tests/cli.rs
git commit -m "feat: scan current directory"
```

### Task 4: Render Onefetch-Inspired Output

**Files:**
- Modify: `Cargo.toml`
- Create: `src/render.rs`
- Modify: `src/lib.rs`
- Modify: `src/main.rs`
- Test: `tests/cli.rs`

- [ ] **Step 1: Write failing render tests**

```rust
// append to tests/cli.rs
#[test]
fn renders_language_breakdown_sorted_by_loc() {
    let temp = assert_fs::TempDir::new().unwrap();
    temp.child("main.rs").write_str("fn main() {}\nfn helper() {}\n").unwrap();
    temp.child("app.py").write_str("print('hi')\n").unwrap();

    let mut cmd = Command::cargo_bin("ploc").unwrap();
    cmd.current_dir(temp.path())
        .arg("--no-color")
        .assert()
        .success()
        .stdout(predicate::str::contains("Languages 2"))
        .stdout(predicate::str::contains("Rust"))
        .stdout(predicate::str::contains("Python"));
}

#[test]
fn no_color_output_has_no_ansi_sequences() {
    let temp = assert_fs::TempDir::new().unwrap();
    temp.child("main.rs").write_str("fn main() {}\n").unwrap();

    let output = Command::cargo_bin("ploc")
        .unwrap()
        .current_dir(temp.path())
        .arg("--no-color")
        .output()
        .unwrap();

    assert!(output.status.success());
    let stdout = String::from_utf8(output.stdout).unwrap();
    assert!(!stdout.contains("\u{1b}["));
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cargo test --test cli`

Expected: FAIL because language rendering is not implemented.

- [ ] **Step 3: Add renderer**

```toml
# Cargo.toml addition
owo-colors = "4.1"
```

```rust
// src/lib.rs
pub mod render;
pub mod scan;
```

```rust
// src/render.rs
use owo_colors::OwoColorize;

use crate::scan::Summary;

pub fn render(summary: &Summary, color: bool) -> String {
    let title = if color {
        summary.root_name.bold().cyan().to_string()
    } else {
        summary.root_name.clone()
    };

    let mut lines = vec![
        format!("╭─ {title}"),
        format!("│ LOC       {}", summary.total_code),
        format!("│ Files     {}", summary.total_files),
        format!("│ Languages {}", summary.languages.len()),
    ];

    if !summary.languages.is_empty() {
        lines.push("│".to_string());
        for language in &summary.languages {
            let percent = if summary.total_code == 0 {
                0.0
            } else {
                (language.code as f64 / summary.total_code as f64) * 100.0
            };
            let name = if color {
                language.name.green().to_string()
            } else {
                language.name.clone()
            };
            lines.push(format!(
                "│ {:<12} {:>7} {:>5.1}%",
                name, language.code, percent
            ));
        }
    }

    lines.push("╰".to_string());
    lines.join("\n") + "\n"
}
```

- [ ] **Step 4: Use renderer in main**

```rust
// src/main.rs
use clap::Parser;
use ploc::{render::render, scan::scan_current_dir};

#[derive(Debug, Parser)]
#[command(version, about = "Count LOC and languages in the current directory")]
struct Cli {
    /// Include dependency, build, VCS, cache, and generated-output directories.
    #[arg(long)]
    include_noise: bool,

    /// Disable ANSI color.
    #[arg(long)]
    no_color: bool,
}

fn main() {
    let cli = Cli::parse();

    match scan_current_dir(cli.include_noise) {
        Ok(summary) => print!("{}", render(&summary, !cli.no_color)),
        Err(message) => {
            eprintln!("ploc: {message}");
            std::process::exit(1);
        }
    }
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `cargo test --test cli`

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add Cargo.toml Cargo.lock src/lib.rs src/main.rs src/render.rs tests/cli.rs
git commit -m "feat: render summary output"
```

### Task 5: Polish Docs and Verify Release

**Files:**
- Create: `README.md`
- Modify: `Cargo.toml`

- [ ] **Step 1: Add README**

```markdown
# ploc

`ploc` is a small Rust CLI that reports lines of code and language usage for exactly the directory you run it from.

## Usage

```sh
ploc
ploc --include-noise
ploc --no-color
```

`ploc` does not discover a git root or package root. It scans the current working directory as-is.

By default it skips common noisy directories such as `.git`, `node_modules`, `target`, `dist`, `build`, and cache directories. Use `--include-noise` to include them.

## Build

```sh
cargo build --release
```

Install the binary somewhere on your `PATH`, for example:

```sh
mkdir -p ~/.local/bin
cp target/release/ploc ~/.local/bin/ploc
```
```

- [ ] **Step 2: Run full verification**

Run: `cargo fmt --check`

Expected: PASS.

Run: `cargo test`

Expected: PASS.

Run: `cargo build --release`

Expected: PASS and `target/release/ploc` exists.

- [ ] **Step 3: Smoke test binary**

Run: `./target/release/ploc --no-color`

Expected: prints a summary for the `ploc` repo.

- [ ] **Step 4: Commit**

```bash
git add README.md Cargo.toml Cargo.lock src tests
git commit -m "docs: add usage and verify release build"
```
